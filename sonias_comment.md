There is a lag in the drawing tool when comment is added to a drawing
when the project is duplicated some of the comments and the drawing were not taken over : 
    - check why this happened
    - correct the bug fixes
    - it looks totally normal when created
    - when resolve the project pin should be hidden on the active page and should show on the resolve tab
    - when on the active tab show just the active comments and when on resolved it should show the 
    - when the comment is resolve the  check how reliably the green buttons works   
- Sonia wants the second image to just continue with the numbers from the first thread in project 